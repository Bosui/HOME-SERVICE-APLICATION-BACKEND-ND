{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Paleisti programą",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "program": "${workspaceFolder}/src/app.js", // Arba nurodykite savo serverio failo kelią
            "env": {
                "NODE_ENV": "development"
            }
        }
    ]
}